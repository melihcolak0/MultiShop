@model List<ResultFeatureDTO>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="content-wrapper">
    <section class="content-header">
        <h3 class="text-primary">
            <i class="fa fa-star"></i> Özellikler
        </h3>
        <ol class="breadcrumb">
            <li><a href="/Admin/Dashboard/Index"><i class="fa fa-home"></i> Dashboard</a></li>
            <li class="active"><i class="fa fa-star"></i> Özellikler</li>
        </ol>
    </section>

    <section class="content container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="chart-box">
                    <h4>Özellikler'in Listelenmesi</h4>
                    <p>Özellikler ile ilgili listeleme, ekleme, silme ve güncelleme işlemlerini aşağıdan gerçekleştirebilirsiniz. Tema bütünlüğü için 4 adet aktif özellik kullanmalısınız.</p>
                    <div class="bs-example" data-example-id="hoverable-table">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>İkon</th>
                                    <th>Başlık</th>
                                    <th>Durum</th>
                                    <th>Aktif/Pasif</th>
                                    <th>Sil</th>
                                    <th>Güncelle</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    count++;
                                    <tr>
                                        <th scope="row">@count</th>
                                        <td>
                                            <i class="@item.IconUrl" style="font-size:40px; color:#444;"></i>
                                        </td>
                                        <td>@item.Title</td>
                                        @if (item.Status)
                                        {
                                            <td><span style="background:#28a745; color:white; padding:3px 10px; border-radius:20px; font-size:12px;">Aktif</span></td>
                                        }
                                        else
                                        {
                                            <td><span style="background:#dc3545; color:white; padding:3px 10px; border-radius:20px; font-size:12px;">Pasif</span></td>
                                        }
                                        @if (item.Status)
                                        {
                                            <td>
                                                <a href="/Admin/Feature/ChangeFeatureStatus/@item.FeatureId"
                                                   class="btn btn-danger btn-rounded btn-sm">
                                                    <i class="bi bi-x"></i> Pasif Yap
                                                </a>
                                            </td>
                                        }
                                        else
                                        {
                                            <td>
                                                <a href="/Admin/Feature/ChangeFeatureStatus/@item.FeatureId"
                                                   class="btn btn-success btn-rounded btn-sm">
                                                    <i class="bi bi-check"></i> Aktif Yap
                                                </a>
                                            </td>
                                        }
                                        <td>
                                            <a href="/Admin/Feature/DeleteFeature/@item.FeatureId"
                                               class="btn btn-danger btn-rounded btn-sm">
                                                <i class="bi bi-trash"></i> Sil
                                            </a>
                                        </td>
                                        <td>
                                            <a href="/Admin/Feature/UpdateFeature/@item.FeatureId"
                                               class="btn btn-warning btn-rounded btn-sm">
                                                <i class="bi bi-pencil-square"></i> Güncelle
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <br />
                        <a href="/Admin/Feature/CreateFeature" class="btn btn-primary btn-rounded btn-sm">
                            <i class="bi bi-plus-circle"></i> Yeni Özellik Ekle
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>
